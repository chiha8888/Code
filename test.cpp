#include<bits/stdc++.h>
using namespace std;
vector<int> edge[1005];
int main(){
	edge[50].push_back(23);
	edge[50].push_back(24);
	edge[50].push_back(25);
	if(find(edge[50].begin(),edge[50].end(),25)!=edge[50].end())
		cout<<"find"<<endl;
	if("aaa"<"aaaa")
	cout<<"yes"<<endl;
	return 0; 
} 
